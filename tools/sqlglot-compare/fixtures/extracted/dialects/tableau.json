{
  "dialect": "tableau",
  "identity": [],
  "transpilation": [
    {
      "sql": "[x]",
      "read": {},
      "write": {
        "hive": "`x`",
        "tableau": "[x]"
      }
    },
    {
      "sql": "\"x\"",
      "read": {},
      "write": {
        "hive": "'x'",
        "tableau": "'x'"
      }
    },
    {
      "sql": "IF x = 'a' THEN y ELSE NULL END",
      "read": {
        "presto": "IF(x = 'a', y, NULL)"
      },
      "write": {
        "presto": "IF(x = 'a', y, NULL)",
        "hive": "IF(x = 'a', y, NULL)",
        "tableau": "IF x = 'a' THEN y ELSE NULL END"
      }
    },
    {
      "sql": "IFNULL(a, 0)",
      "read": {
        "presto": "COALESCE(a, 0)"
      },
      "write": {
        "presto": "COALESCE(a, 0)",
        "hive": "COALESCE(a, 0)",
        "tableau": "IFNULL(a, 0)"
      }
    },
    {
      "sql": "COUNTD(a)",
      "read": {
        "presto": "COUNT(DISTINCT a)"
      },
      "write": {
        "presto": "COUNT(DISTINCT a)",
        "hive": "COUNT(DISTINCT a)",
        "tableau": "COUNTD(a)"
      }
    },
    {
      "sql": "COUNTD((a))",
      "read": {
        "presto": "COUNT(DISTINCT(a))"
      },
      "write": {
        "presto": "COUNT(DISTINCT (a))",
        "hive": "COUNT(DISTINCT (a))",
        "tableau": "COUNTD((a))"
      }
    },
    {
      "sql": "COUNT(a)",
      "read": {
        "presto": "COUNT(a)"
      },
      "write": {
        "presto": "COUNT(a)",
        "hive": "COUNT(a)",
        "tableau": "COUNT(a)"
      }
    }
  ]
}